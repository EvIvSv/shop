<?phpinclude_once('Model/startup.php');include_once('Route.php');include_once('Model/M_Users.php');// Установка параметров, подключение к БД, запуск сессии.startup();// Кодировка.header('Content-type: text/html; charset=windows-1251');//if (isset($_COOKIE['login']))	//			M_Users::Instance()->Login($_COOKIE['login'], $_COOKIE['pass'], true);$router = new Route;	// Создаем объект, который будет искать нужные контроллеры.$router->Run();			// Начинаем поиск нужного контроллера.?>